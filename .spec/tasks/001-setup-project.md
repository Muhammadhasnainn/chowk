# Task 001: Project Setup

## Status
Completed

## Priority
High

## Description
Initialise the Next.js 14 App Router project with TypeScript and Tailwind CSS, install
dependencies, and commit the `.spec/` directory.

## Acceptance Criteria
- [x] `npm run dev` serves the app on port 3000
- [x] TypeScript strict mode enabled with the `@/*` path alias
- [x] Tailwind compiles and picks up `app/` and `components/`
- [x] lucide-react, clsx, tailwind-merge and class-variance-authority installed
- [x] Git repository initialised with `.gitignore` covering `node_modules`, `.next`, `.env*`

## Time Estimation
2 hours
